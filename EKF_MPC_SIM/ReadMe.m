


    disp("Adjust Plant Vehicle Parameters using the setupVehicleParams script")
    disp("Adjust Lat Controller Parameters using the setupLatMpcParams script")
    disp("VEHICLE class owns all the plant parameters and methods")
    disp("MPC_CONTROLLER class owns all the MPC parameters and methods")
    disp("MRAC_CONTROLLER class owns all the MRAC parameters and methods")
    disp("updateLonMpc method implements the update of the conic lon mpc problem")
    disp("updateLatMpc method implements the update of the conic lat mpc problem")
    disp("ReferenceGenerator method implents the reference for the controllers")
    disp("calc_error_states implements the state to error states conversion")

